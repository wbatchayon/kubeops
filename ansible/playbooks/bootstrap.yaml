---
# Bootstrap playbook for Kubernetes cluster nodes
# This playbook prepares all nodes for Kubernetes installation

- name: Bootstrap Kubernetes Nodes
  hosts: all
  become: true
  gather_facts: true
  vars:
    # Kubernetes version to install
    kubernetes_version: "1.28.0"

    # Container runtime
    container_runtime: "containerd"

    # Network settings
    pod_network_cidr: "10.244.0.0/16"
    service_cidr: "10.96.0.0/16"
    cluster_domain: "cluster.local"

    # Kernel modules and sysctl parameters are defined as defaults of the
    # common role (ansible/roles/common/defaults/main.yaml) and can be
    # overridden here if needed.

  pre_tasks:
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600
      changed_when: false
      when: ansible_os_family == "Debian"

    - name: Check if reboot is required
      ansible.builtin.stat:
        path: /var/run/reboot-required
      register: reboot_required
      failed_when: false
      changed_when: false

  tasks:
    # Common tasks for all nodes
    - name: Include common role
      ansible.builtin.import_role:
        name: common

    # Node firewall: default drop, only 443 admitted from outside
    - name: Include firewall role
      ansible.builtin.import_role:
        name: firewall

    # Container runtime tasks
    - name: Include containerd role
      ansible.builtin.import_role:
        name: containerd

    # Kubernetes components
    - name: Include kubeadm role
      ansible.builtin.import_role:
        name: kubeadm

  post_tasks:
    - name: Verify kubeadm is installed
      ansible.builtin.command: kubeadm version
      register: kubeadm_version
      changed_when: false
      failed_when: false

    - name: Display kubeadm version
      ansible.builtin.debug:
        msg: "kubeadm version: {{ kubeadm_version.stdout }}"

    - name: Verify containerd is running
      ansible.builtin.systemd:
        name: containerd
        state: started
        enabled: true

    - name: Display node information
      ansible.builtin.debug:
        msg: |
          Node: {{ inventory_hostname }}
          Role: {{ kubernetes_role | default('worker') }}
          IP: {{ ansible_host }}
